# Port configuration (default 3000)
PORT=3000

# MongoDB Connection URI (optional: leave blank to automatically use the embedded in-memory MongoDB, or specify a MongoDB Atlas / external URI)
MONGODB_URI=

# JWT Secret key for authentication token signing
JWT_SECRET="fleet_management_academic_jwt_secret_key_2025"

